# Third-Party Evidence

This directory is reserved for controlled source snapshots, rights notices, approval records, and obligation evidence referenced by `third-party-materials.yaml`.

An entry is accepted only when the manifest validator binds the registered repository file and retained source snapshot to their actual SHA-256 values. Partial-file exceptions are not supported; differently licensed content shall be isolated into a separate file.
